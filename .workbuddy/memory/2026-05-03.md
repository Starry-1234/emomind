# 2026-05-03 工作记录

## emomind 项目 P0 问题修复

### 修复内容

1. **users.py** - 移除新账户邮件中的密码参数
   - 删除 `password=user_in.password` 调用参数
   - 修复 `read_user_by_id` 权限检查顺序（先检查 user is None，再检查权限）

2. **utils.py** - 移除密码参数 + 修复 assert
   - `generate_new_account_email` 函数移除 `password: str` 参数
   - 从 context 字典中移除 `"password": password`
   - `assert settings.emails_enabled` 改为 `if not settings.emails_enabled: raise ValueError(...)`

3. **new_account.mjml** - 删除密码显示行
   - 移除模板中 `Password: {{ password }}` 行

4. **.gitignore** - 添加敏感文件忽略规则
   - 添加 `.env`、`frontend/.env`、`backend/uploads/`

5. **创建 .env.example 文件**
   - 根目录和 `frontend/` 各创建一个，包含占位符而非真实凭据

6. **ai-doctor.tsx** - 用真实 Dify API 替代 Mock 分析
   - 重写"开始分析"按钮 onClick 处理器：上传文件 → sendMessageStream 获取真实分析 → 保存到数据库
   - 删除 `generateMockAnalysis` 函数

7. **删除过时的邮件模板缓存**
   - 删除 `backend/app/email-templates/build/new_account.html`

### 注意
- `new_account.html` 需要重新从 MJML 源码构建
- 用户需自行将 `.env` 中的默认/真实凭据改为安全值
- `frontend/.env` 中的 Dify API Key 已添加到 .gitignore，但文件中真实 Key 仍需用户更换

## P1 问题修复

### 修复内容

1. **test.tsx** - 测评记录保存 bug（两个 bug 叠加）
   - P1-4: `if (!accumulated.includes("RESULT_JSON::")) return` 永远为 true（Dify 回复不含该前缀）→ 移除该条件，测评完成后直接保存
   - P1-5: `activeTest` 和 `testAnswers` 在闭包中被清空 → 在清除状态前先捕获值 `capturedTest`/`capturedAnswers`，闭包中使用捕获值

2. **login.tsx** - `redirect()` 被 `catch` 吞掉
   - 缩窄 try/catch 范围，只捕获 `readUserMe()`，redirect() 在 try 外部抛出，不被捕获

3. **compose.yml** - `postgres:18` 不存在
   - 改为 `postgres:17`

4. **__root.tsx** - DevTools 在生产环境可见
   - DevTools 组件用 `{import.meta.env.DEV && (...)}` 包裹

5. **chat-history.tsx** - AI 回复不显示
   - 条件 `!msg.query && msg.answer` 改为 `msg.answer`，允许同时显示 query+answer 的消息

6. **_admin-layout.tsx** + **route.tsx** - `redirect()` 被 `catch` 吞掉（导致 404 的根本原因）
   - 同 login.tsx，缩窄 try/catch 范围，只捕获 `readUserMe()` 的 API 错误，redirect() 不被捕获

### 404 页面问题
- 根因：`_admin-layout.tsx` 和 `route.tsx` 的 `beforeLoad` 中 `redirect()` 被 `catch` 吞掉，导致认证重定向被误判为 token 失效
- 修复后：超管访问 `/admin/*` 正确重定向，普通用户访问 `/user/*` 正确重定向
- 链接路径本身正确：`/admin/admin-test-records`（文件 `admin-test-records.tsx`）、`/user/consultations`


# 需求规格说明书

## 1. 引言

### 1.1 编写目的

本文档用于描述 EmoMind 心理测评平台的功能需求、非功能需求及系统约束，为后续概要设计、详细设计和开发实施提供依据。

### 1.2 项目背景

EmoMind 是一个面向个人用户和管理员的心理测评平台。平台提供用户注册登录、心理测评记录管理、文件分析报告生成、AI 心理问答咨询等功能。管理员可通过仪表盘查看平台统计数据。

### 1.3 术语定义

| 术语 | 说明 |
|------|------|
| JWT | JSON Web Token，用于用户认证的令牌机制 |
| SSE | Server-Sent Events，服务器向客户端推送实时数据的技术 |
| LangGraph | 基于 LangChain 的有状态 AI 工作流编排框架（本项目 AI 核心） |
| ai-runtime | Python 边车服务，运行 LangGraph 工作流 |
| AiProxyService | Spring 端 AI 代理，转发请求到 ai-runtime |
| PostgresSaver | LangGraph 官方 Postgres checkpoint 持久化后端 |
| Streak | 用户连续活跃天数 |
| 分析报告 | 对用户上传文件进行心理分析后生成的报告 |
| 测评记录 | 用户完成心理测评后保存的题目、答案和得分记录 |

### 1.4 参考资料

- Spring Boot 官方文档
- Spring Security 参考手册
- OpenAPI 3.0 规范
- LangGraph 官方文档
- ai-runtime 服务契约（见 doc/langgraph-migration/02-components.md）

---

## 2. 总体描述

### 2.1 系统边界

EmoMind 系统包含以下组成部分：
- Web 前端应用（React）
- REST API 后端服务（Spring Boot）
- Python AI 边车（ai-runtime，基于 LangGraph）
- PostgreSQL 数据库（含 pgvector 扩展）
- Redis（会话状态与缓存，M4+）

### 2.2 用户角色

| 角色 | 说明 | 权限 |
|------|------|------|
| 普通用户 | 注册并登录的个人用户 | 管理自己的资料、分析报告、测评记录；使用 AI 咨询 |
| 超级用户（管理员） | 平台管理员 | 普通用户所有权限 + 查看统计数据、管理所有用户和测评记录 |

### 2.3 运行环境

**服务端：**
- Java 17 (LTS)
- Spring Boot 3.2
- PostgreSQL 17
- Docker & Docker Compose

**客户端：**
- 现代浏览器（Chrome, Firefox, Safari, Edge）
- 支持 JavaScript 和 WebSocket/SSE

### 2.4 约束条件

- API 接口需兼容现有前端自动生成的 OpenAPI 客户端
- 数据库存储使用无时区 TIMESTAMP，应用层统一使用 CST (Asia/Shanghai)
- 密码哈希使用 BCrypt 算法
- JWT 签名使用 HS256 算法
- LLM Provider API Key 由 ai-runtime 服务端管理，不暴露给前端或 Spring 端

---

## 3. 功能需求

### 3.1 认证模块

#### 3.1.1 用户注册

- **需求编号**: AUTH-001
- **需求描述**: 用户通过邮箱和密码注册账号
- **输入**: 邮箱（唯一）、密码、全名（可选）
- **处理**: 验证邮箱格式唯一性，密码加密存储，创建用户记录
- **输出**: 新用户信息
- **异常**: 邮箱已存在、邮箱格式无效、密码过短（最少 8 位）

#### 3.1.2 用户登录

- **需求编号**: AUTH-002
- **需求描述**: 用户通过邮箱和密码登录，获取 JWT 访问令牌
- **输入**: 邮箱、密码（OAuth2 Password Flow）
- **处理**: 验证邮箱和密码，生成 JWT Token
- **输出**: access_token, token_type
- **异常**: 邮箱不存在、密码错误、账号未激活

#### 3.1.3 Token 验证

- **需求编号**: AUTH-003
- **需求描述**: 验证 JWT Token 的有效性并返回用户信息
- **输入**: Authorization: Bearer {token}
- **处理**: 解析并验证 JWT，查询用户信息
- **输出**: 用户基本信息
- **异常**: Token 过期、Token 无效、用户不存在

#### 3.1.4 密码重置

- **需求编号**: AUTH-004
- **需求描述**: 用户请求密码重置，系统发送重置邮件
- **输入**: 用户邮箱
- **处理**: 生成重置 Token（48 小时有效），发送含重置链接的邮件
- **输出**: 邮件发送成功提示（无论邮箱是否存在，返回相同响应）
- **异常**: 邮件服务不可用

#### 3.1.5 密码修改

- **需求编号**: AUTH-005
- **需求描述**: 用户通过重置 Token 或当前密码修改密码
- **输入**: 当前密码 + 新密码 或 重置 Token + 新密码
- **处理**: 验证当前密码或重置 Token，更新密码哈希
- **输出**: 修改成功提示
- **异常**: 当前密码错误、Token 无效或过期、新密码不符合要求

#### 3.1.6 连续活跃天数更新

- **需求编号**: AUTH-006
- **需求描述**: 每次认证请求自动更新用户的连续活跃天数
- **处理规则**:
  - 首次活跃：streak_days = 1
  - 连续活跃（昨天有记录）：streak_days += 1
  - 中断后重新活跃：streak_days = 1
  - 同一天多次请求：不更新
- **时区**: Asia/Shanghai

### 3.2 用户管理模块

#### 3.2.1 获取当前用户信息

- **需求编号**: USER-001
- **需求描述**: 获取当前登录用户的详细信息
- **输出**: 用户 ID、邮箱、全名、是否激活、是否超级用户、streak_days、last_active_date

#### 3.2.2 更新当前用户信息

- **需求编号**: USER-002
- **需求描述**: 更新当前用户的邮箱或全名
- **输入**: 邮箱（可选）、全名（可选）
- **约束**: 邮箱修改时不能与其他用户重复

#### 3.2.3 删除当前用户

- **需求编号**: USER-003
- **需求描述**: 用户删除自己的账户
- **约束**: 删除用户时级联删除其所有关联数据（分析报告、测评记录）

#### 3.2.4 获取所有用户列表

- **需求编号**: USER-004
- **需求描述**: 管理员获取平台所有用户列表（分页）
- **输入**: skip, limit
- **权限**: 仅超级用户
- **输出**: 用户列表 + 总数

#### 3.2.5 创建用户

- **需求编号**: USER-005
- **需求描述**: 管理员创建新用户
- **输入**: 邮箱、密码、全名、是否超级用户
- **权限**: 仅超级用户

#### 3.2.6 更新指定用户

- **需求编号**: USER-006
- **需求描述**: 管理员更新指定用户信息
- **输入**: 用户 ID + 可更新字段
- **权限**: 仅超级用户

#### 3.2.7 删除指定用户

- **需求编号**: USER-007
- **需求描述**: 管理员删除指定用户
- **输入**: 用户 ID
- **权限**: 仅超级用户

### 3.3 文件分析报告模块

#### 3.3.1 创建分析报告

- **需求编号**: ANAL-001
- **需求描述**: 创建新的文件分析报告
- **输入**: 文件名、文件类型、文件大小、分析结果文本、对话 ID（可选）
- **约束**: 自动关联当前登录用户
- **输出**: 创建成功的分析报告信息

#### 3.3.2 获取分析报告列表

- **需求编号**: ANAL-002
- **需求描述**: 获取当前用户的分析报告列表（分页）
- **输入**: skip, limit
- **输出**: 分析报告列表 + 总数

#### 3.3.3 获取分析报告详情

- **需求编号**: ANAL-003
- **需求描述**: 获取单个分析报告的详细信息
- **输入**: 报告 ID
- **约束**: 只能查看自己创建的报告
- **输出**: 分析报告详细信息

#### 3.3.4 删除分析报告

- **需求编号**: ANAL-004
- **需求描述**: 删除指定的分析报告
- **输入**: 报告 ID
- **约束**: 只能删除自己创建的报告
- **输出**: 删除成功提示

### 3.4 心理测评记录模块

#### 3.4.1 创建测评记录

- **需求编号**: TEST-001
- **需求描述**: 创建新的心理测评记录
- **输入**: 测评名称、用户主题（可选）、题目列表(JSON)、答案列表(JSON)、总得分、满分、结果描述、评分区间(JSON)、对话 ID（可选）
- **约束**: 自动关联当前登录用户
- **输出**: 创建成功的测评记录信息

#### 3.4.2 获取测评记录列表

- **需求编号**: TEST-002
- **需求描述**: 获取当前用户的测评记录列表（分页）
- **输入**: skip, limit
- **输出**: 测评记录列表 + 总数

#### 3.4.3 获取测评记录详情

- **需求编号**: TEST-003
- **需求描述**: 获取单个测评记录的详细信息
- **输入**: 记录 ID
- **约束**: 普通用户只能查看自己的记录，管理员可查看所有
- **输出**: 测评记录详细信息（含题目、答案、评分）

#### 3.4.4 更新测评记录

- **需求编号**: TEST-004
- **需求描述**: 更新指定的测评记录
- **输入**: 记录 ID + 可更新字段
- **约束**: 只能更新自己创建的记录
- **输出**: 更新后的测评记录信息

#### 3.4.5 删除测评记录

- **需求编号**: TEST-005
- **需求描述**: 删除指定的测评记录
- **输入**: 记录 ID
- **约束**: 普通用户只能删除自己的记录，管理员可删除所有
- **输出**: 删除成功提示

#### 3.4.6 管理员获取所有测评记录

- **需求编号**: TEST-006
- **需求描述**: 管理员获取平台所有测评记录
- **输入**: skip, limit, user_id（可选过滤）
- **权限**: 仅超级用户
- **输出**: 测评记录列表 + 总数

#### 3.4.7 管理员删除测评记录

- **需求编号**: TEST-007
- **需求描述**: 管理员删除任意测评记录
- **输入**: 记录 ID
- **权限**: 仅超级用户
- **输出**: 删除成功提示

### 3.5 AI 集成模块（LangGraph + ai-runtime）

> M0 里程碑：本节由 M1+ 阶段实施；M0 仅提供 `AiController.healthz()` 占位与 AiProxyService 桩。
> 详见 `doc/langgraph-migration/00-overview.md` 与 `doc/langgraph-migration/02-components.md`。

#### 3.5.1 发送聊天消息（SSE 流式）

- **需求编号**: AI-001
- **需求描述**: 用户发送聊天消息，前端通过 Spring 端 AiController 进入 AiProxyService，AiProxyService 转发到 ai-runtime（LangGraph 工作流），ai-runtime 以 LangGraph 原生 SSE 事件流回传
- **输入**: 用户消息内容、conversation_id（可选，新建则为空）、user_id（来自 JWT）
- **处理**: AiProxyService 调用 ai-runtime 的 chat stream 端点，传递 JWT 解析后的 user_id；ai-runtime 加载对应 thread 的 checkpoint，按图执行节点，回传 token/事件流
- **输出**: SSE 流（LangGraph 原生事件格式：messages/values/updates/end）
- **约束**: 需要用户认证；M1 起强制

#### 3.5.2 多模态输入（图像/音频）

- **需求编号**: AI-002
- **需求描述**: 用户上传图像或音频文件用于多模态心理评估
- **输入**: 文件（multipart）、conversation_id
- **处理**: ai-runtime 调用 Qwen3-Omni（默认）或 MinMax 多模态模型（M3+ 实施）
- **输出**: 多模态回复流
- **约束**: 需要用户认证；M3 起强制

#### 3.5.3 心理测评工作流入口

- **需求编号**: AI-003
- **需求描述**: 前端调用测评工作流入口触发 LangGraph 测评图
- **输入**: user_id、test_type
- **处理**: ai-runtime 加载对应测试类型的 LangGraph 图，按节点出题/收答/评分
- **输出**: 测评题目流 + 最终结果
- **约束**: M2 实施

#### 3.5.4 获取对话列表

- **需求编号**: AI-004
- **需求描述**: 获取当前用户的对话列表（基于 `conversation_meta` 表）
- **输入**: user_id（来自 JWT）
- **输出**: 对话列表（含 title、最后活跃时间、消息计数）
- **约束**: 需要用户认证

#### 3.5.5 获取消息历史

- **需求编号**: AI-005
- **需求描述**: 获取指定对话的历史消息
- **输入**: conversation_id、user_id（来自 JWT）
- **输出**: 消息列表（按时间排序）
- **约束**: 只能查看自己的对话

#### 3.5.6 删除对话

- **需求编号**: AI-006
- **需求描述**: 删除指定对话及其 LangGraph checkpoint
- **输入**: conversation_id、user_id（来自 JWT）
- **输出**: 删除结果
- **约束**: 只能删除自己的对话；同步删除 PostgresSaver 中的 thread state

### 3.6 管理员统计模块

#### 3.6.1 获取统计数据

- **需求编号**: ADMIN-001
- **需求描述**: 获取管理员仪表盘统计数据
- **权限**: 仅超级用户
- **输出**:
  - 用户总数
  - 测评记录总数
  - 分析报告总数
  - 今日新增用户数
  - 今日新增测评记录数
  - 今日新增分析报告数

### 3.7 健康检查模块

#### 3.7.1 服务健康检查

- **需求编号**: HEALTH-001
- **需求描述**: 检查后端服务是否正常运行
- **输出**: 服务状态（ok/error）
- **用途**: Docker 健康检查、负载均衡器健康检查

---

## 4. 非功能需求

### 4.1 性能需求

| 需求编号 | 需求描述 | 指标 |
|---------|---------|------|
| PERF-001 | API 响应时间 | P95 < 200ms（不含 ai-runtime LLM 调用） |
| PERF-002 | 并发用户数 | 支持 ≥ 100 同时在线用户 |
| PERF-003 | 数据库连接池 | 最小 5，最大 20 |
| PERF-004 | 页面加载时间 | 首屏加载 < 3 秒 |

### 4.2 安全需求

| 需求编号 | 需求描述 | 措施 |
|---------|---------|------|
| SEC-001 | 密码安全 | BCrypt 哈希存储，强度 ≥ 10 |
| SEC-002 | 传输安全 | HTTPS 强制，HSTS 头部 |
| SEC-003 | 认证机制 | JWT Bearer Token，HS256 签名 |
| SEC-004 | Token 有效期 | 8 天 |
| SEC-005 | CORS 限制 | 仅允许配置的域名访问 |
| SEC-006 | 输入验证 | 所有接口参数服务端校验 |
| SEC-007 | SQL 注入防护 | 使用参数化查询（JPA） |
| SEC-008 | XSS 防护 | 前端转义 + CSP 头部 |
| SEC-009 | 敏感信息 | API Key 不在日志中输出 |

### 4.3 可用性需求

| 需求编号 | 需求描述 | 指标 |
|---------|---------|------|
| AVAIL-001 | 服务可用性 | ≥ 99.5% |
| AVAIL-002 | 数据库备份 | 每日自动备份 |
| AVAIL-003 | 故障恢复 | 支持滚动重启，无中断部署 |

### 4.4 可维护性需求

| 需求编号 | 需求描述 |
|---------|---------|
| MAINT-001 | 代码覆盖率 ≥ 80% |
| MAINT-002 | API 文档自动生成（OpenAPI） |
| MAINT-003 | 数据库迁移版本化管理（Flyway） |
| MAINT-004 | 统一日志格式，支持结构化日志 |
| MAINT-005 | 配置文件外部化，支持环境变量注入 |

---

## 5. 数据需求

### 5.1 数据实体

系统涉及以下核心实体：

1. **用户（User）**：平台注册用户，包含认证信息和个人资料
2. **文件分析报告（FileAnalysisReport）**：用户上传文件的分析结果
3. **测评记录（TestRecord）**：用户完成心理测评的记录

### 5.2 数据流

**用户注册登录流：**
```
用户 → 注册/登录 → 后端验证 → 生成 JWT → 返回 Token → 后续请求携带 Token
```

**分析报告创建流：**
```
用户上传文件 → 前端处理 → 调用创建 API → 后端保存 → 返回报告 ID
```

**AI 咨询流：**
```
用户发送消息 → Spring AiController → AiProxyService → ai-runtime (LangGraph)
     → ai-runtime 调用 LLM Provider（MinMax / Qwen3-Omni）
     → ai-runtime 回传 LangGraph SSE 事件 → AiProxyService 透传 → 前端 useChat 渲染
```

**测评记录流：**
```
用户完成测评 → 前端提交题目/答案/得分 → 后端保存 → 返回记录详情
```

---

## 6. 接口需求

### 6.1 用户接口

- Web 浏览器访问前端应用
- 支持响应式布局，适配桌面和移动端
- 支持主流浏览器最新两个版本

### 6.2 外部接口（ai-runtime / LLM Providers）

> 本分支不再使用 Dify。AI 编排改为内部 ai-runtime Python 边车，对外只暴露 Spring 端 `/api/v1/ai/*`。
> LLM Provider 由 ai-runtime 内部调用，对 Spring 与前端均不可见。

**Spring 端 AI 接口（对前端）：**

| 接口 | 方法 | 说明 |
|------|------|------|
| /api/v1/ai/healthz | GET | ai-runtime 健康检查（公开） |
| /api/v1/ai/chat | POST | 发送聊天消息，SSE 流式返回 |
| /api/v1/ai/chat/multimodal | POST | 多模态输入（M3+） |
| /api/v1/ai/conversations | GET | 获取对话列表 |
| /api/v1/ai/conversations/{id}/messages | GET | 获取消息历史 |
| /api/v1/ai/conversations/{id} | DELETE | 删除对话 |

**ai-runtime 内部接口（对 Spring）：**

| 接口 | 方法 | 说明 |
|------|------|------|
| /chat/stream | POST | LangGraph 工作流入口，SSE 流式回传 |
| /healthz | GET | 边车健康检查 |
| /threads/{id}/state | GET | 读取 PostgresSaver checkpoint |

**认证方式**: Spring 端使用 JWT；Spring → ai-runtime 使用内部共享密钥（`LANGGRAPH_SHARED_SECRET`）
**LLM Provider 凭据**: 仅 ai-runtime 持有，注入到 `LANGGRAPH_*` / `MINIMAX_*` / `QWEN_OMNI_*` / `EMBEDDING_*` 环境变量
**数据传输**: JSON
**流式接口**: text/event-stream（LangGraph 原生事件）
